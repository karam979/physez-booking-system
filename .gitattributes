# Files that run on the Linux VPS must keep LF endings — a CRLF shebang makes
# bash fail with "bad interpreter", and the config parsers are just as picky.
*.sh text eol=lf
*.cron text eol=lf
*.service text eol=lf
*.conf text eol=lf
Caddyfile text eol=lf
